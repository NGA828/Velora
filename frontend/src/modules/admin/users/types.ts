export interface SystemUser { id: string; email: string; first_name: string; last_name: string; full_name: string; phone: string; role: string; role_label: string; employee_number: string | null; is_active: boolean; must_change_password: boolean; last_login: string | null; date_joined: string }
export interface AuditEvent { id: string; created_at: string; actor_name: string | null; actor_email: string | null; action: string; object_type: string; object_id: string; reason: string; request_id: string; ip_address: string | null }
export interface SystemDashboard { database: { healthy: boolean; engine: string }; scheduler: { healthy: boolean; last_seen_at: string | null }; integrations: { twilio_configured: boolean; smtp_configured: boolean }; users: { active: number; inactive: number; by_role: { role: string; count: number }[] }; security: { failed_logins_24h: number; audit_events_24h: number }; failures: { transfer_email: number; twilio_webhooks: number } }
